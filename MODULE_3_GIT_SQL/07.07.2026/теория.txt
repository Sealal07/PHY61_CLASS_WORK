 tab1
INNER JOIN tab2
ON

 
2) LEFT JOIN - все строки из левой (первой)
таблицы и подходящие строки из правой таблицы.
если совпадений в правой нет, то вернется NULL







SELECT t1.col, t2.col 
FROM table1 t1
LEFT JOIN table2 t2
ON t1.id = t2.t1_id 
118X351
nXm 1000000
n 500
m 2000

ФУНКЦИИ АГРЕГАЦИИ - из множества строк
в процессе вычисления возвращает
один результат


COUNT()-считает кол-во строк
SUM()-считает сумму всех чисел в колонке
MIN() MAX()-находит минимальное/максимальное в колонке
AVG()-среднее арифметическое в колонке

Используются в SELECT
SELECT FUNC(col_name) FROM table_name
WHERE condition;

Игнорируют значение NULL (кроме COUNT(*))
Группировка GROUP BY 


ORDER BY - сортировка ASC-по возрастанию/DESK по убыванию
WHERE - отсекает лишние строки ДО группировки
HAVING - фильтрует результат ПОСЛЕ группировки


1)SELECT (что выбираем)
2)FROM (из какой таблицы)
3)WHERE (фильтрация строк ДО группировки)
4)GROUP BY (группируем)
5)HAVING (фильтрация после группировки)
6)ORDER BY (сортирвока результата)
7)LIMIT (ограничение кол-ва строк)




